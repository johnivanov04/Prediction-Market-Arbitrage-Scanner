"""Command-line interface (Typer).

Planned commands: ``sync-metadata``, ``capture-live``, ``scan-live``,
``replay``, ``audit-opportunity``, ``validate-books``, ``list-relations``,
``verify-relation``.

There is deliberately no command that places, modifies or cancels an order.
"""
