# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/
.env
.env.*
!.env.example

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---------------------------------------------------------------------------
# Secrets: Kalshi API keys are RSA private keys. Never commit them.
# ---------------------------------------------------------------------------
*.pem
*.key
secrets/
keys/

# ---------------------------------------------------------------------------
# Captured data. The raw journal is append-only research data and can be very
# large; it is reproduced by re-capture, never by git.
# ---------------------------------------------------------------------------
data/
raw/
captures/
*.parquet
*.duckdb
*.sqlite

# Experiment run artefacts. Regenerated by tools/ws_sequence_experiment.py;
# the durable evidence lives in docs/api_assumptions.md and the fixtures.
ws_experiment_results.json
reconstruction_validation.json
execution_validation.json

# OS / editor
.DS_Store
.idea/
.vscode/
fee_validation.json
binary_complement_scan.json
certificate_review_requests.json

# Settlement evidence and certificates are local research records. Contract
# documents belong to the exchange; this repository is not a mirror of them.
data/semantics/
relation_discovery.json
data/replay/
replay_comparison.json

# Step 11 research output. Derived from public read-only data and
# regenerated by tools/research_*.py; the durable findings live in
# docs/api_assumptions.md and docs/exhaustiveness.md.
membership_research.json
exhaustiveness_research.json
mutually_exclusive_study.json
yes_basket_scan.json
soak_report.json
review_queue.json
security_scan.json
data/catalogue/
